# Marketo

Production website: https://marketohub.online

The frontend is deployed as the `ecommerce-web` Cloudflare Worker. Render must use
`FRONTEND_URL=https://marketohub.online`; bearer-token authentication remains unchanged.
